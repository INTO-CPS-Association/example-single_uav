class lib_MidacoWrapperProxy

operations
	public init : () ==> ()
	init() == is not yet specified;
	
	public solve: ((seq of real) * (seq of real)) ==> (real * real * real)
	solve(params) == is not yet specified;
	
	public calculate_f: ((seq of real) * (seq of real) * (seq of real)) ==> (real * real * real)
	calculate_f(params) == is not yet specified;
	
end lib_MidacoWrapperProxy