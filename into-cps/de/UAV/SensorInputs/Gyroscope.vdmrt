class Gyroscope
types

values

instance variables
	-- contract connections
	velocityYaw : real := 0;
	velocityPitch : real := 0;
	velocityRoll : real := 0;
	
	-- Sampled data
	-- x = roll, y = pitch, z := yaw 
	velocityRs : Types`Vector3 := mk_Types`Vector3(0,0,0); -- r/s

operations
	public update : () ==> ()
	update() == velocityRs := mk_Types`Vector3(velocityRoll,velocityPitch,velocityYaw);
	
	public getVelocity : () ==> Types`Vector3
	getVelocity() == return velocityRs;
	

functions

traces

end Gyroscope