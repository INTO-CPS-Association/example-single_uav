class Motor
types
values

instance variables
	-- Contract connection
	throttle : Types`Throttle := 0; -- [% of full throttle]
	
operations
	public setThrottle : real ==> ()
	setThrottle(thr) == throttle := thr
	pre Types`throttlePre(thr);

functions

traces

end Motor