class SF_GPS
instance variables
	GPS : GPS;
operations
	public SF_GPS : GPS ==> SF_GPS
	SF_GPS(gpsComponent) ==
		GPS := gpsComponent;
	
	public getPosition : () ==> Types`Vector3
	getPosition() == return GPS.getLocation();
	
	public getVelocity : () ==> Types`Vector3
	getVelocity() == return GPS.getVelocity();
		
end SF_GPS